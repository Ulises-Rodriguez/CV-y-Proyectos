# Mongo DB

'''
Python es una tecnología backend y se puede conectar con diferentes aplicaciones de bases de datos. Se puede conectar a bases de datos SQL y noSQL. En esta sección, conectamos Python con la base de datos MongoDB, que es una base de datos noSQL.

267 / 5.000
Resultados de traducción
MongoDB es una base de datos NoSQL. MongoDB almacena datos en un documento similar a JSON que hace que MongoDB sea muy flexible y escalable.
'''
# from flask import Flask, render_template
# import os
# MONGODB_URI = 'mongodb+srv://Ulises:Mrmishu2@30diaspython.jjnqbe8.mongodb.net/?retryWrites=true&w=majority'
# client = pymongo.MongoClient(MONGODB_URI)
# print(client.list_database_names())

# app = Flask(__name__)
# if __name__ == '__main__':
#     # for deployment we use the environ
#     # to make it work for both production and development
#     port = int(os.environ.get("PORT", 5000))
#     app.run(debug=True, host='0.0.0.0', port=port)
