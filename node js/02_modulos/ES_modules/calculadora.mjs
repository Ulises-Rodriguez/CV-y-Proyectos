const sumar = (a,b)=>{
    return a+b;
}

const restar = (a,b) => a-b;

const multiplicar = (a,b) => a*b;

const dividir = (a,b) => a/b;

export const calculadora = {
    sumar : sumar,
    restar,
    multiplicar,
    dividir
}